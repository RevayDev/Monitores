# Deploy en Servidor (MySQL 8)

1. `npm run install:all`
2. Configurar `backend/.env`
3. `npm run db:init`
4. `npm run db:seed:roots`
5. `npm run db:check`
6. `npm run build`
7. `npm run start`

Si `db:check` falla, no continuar deploy.
